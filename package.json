{
  "name": "beck-marketplace-project",
  "version": "1.0.0",
  "description": "Beck Marketplace Chrome Extension and SFTP Proxy Server",
  "private": true,
  "scripts": {
    "start": "cd server && npm install && npm start"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}